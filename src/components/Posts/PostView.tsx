import React, { Component } from "react";

interface Props {
    id: number;
    text: string;
}

export default class PostView extends Component {
    render() {
        return (
            <div/>
        );
    }
}
