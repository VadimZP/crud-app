import React, { Component } from "react";

interface Props {
    id: number;
    text: string;
}

export default class Post extends Component {
    render() {
        return (
            <div/>
        );
    }
}
